{
  "devDependencies": {
    "cypress": "^13.6.6"
  },
  "scripts": {
    "test": "cypress run --headless --browser chrome",
    "remove-results": "rm -rf cypress/screenshots/* && rm -rf cypress/videos/*"
  }
}
